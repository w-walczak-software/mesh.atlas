package pl.com.ww.mesh.atlas.api.domain.model;

public enum SubscriptionSource {
    /** Subscription created through the mesh.atlas internal portal. */
    INTERNAL_PORTAL,
    /** Subscription created through the public developer portal. */
    DEVELOPER_PORTAL
}
